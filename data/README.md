# Data directory

`privacy_dataset.csv` is a tiny synthetic starter dataset for development and CI.

For serious experiments, replace it with a larger synthetic/anonymized corpus and document its provenance, annotation guidelines, train/validation/test split, class balance and licensing. Never commit real personal information to this public repository.
